export interface iconManagerProps {
  type: string;
  name: string;
}

// const iconManager = ({ props }: { props: iconManagerProps }) => {
//   let Icon: object;
//   switch (props.type) {
//     case 'fa':
//       const { [props.name]: string } = Icons;
//       break;

//     default:
//       break;
//   }
// };

// export { iconManager };
